# 세션 인수인계 — RAG Phase 2 검증 완료 (2026-08-21 갱신)

담당: A(황재원). 정식 검증 시나리오는 `docs/검증-시나리오.md`, 레드팀 항목은
`docs/레드팀-체크리스트.md` 참고 — 이 문서는 "지금 어디까지 됐고 뭐가 막혀 있는지"만 다룬다.

## 한 줄 상태

**RAG Phase 2(학사규정 PDF 3종 인제스트) 임베딩 실측 검증까지 완료했다.**
새 `OPENAI_API_KEY`로 실제 임베딩 호출이 성공했고, 6단계 검증을 전부 통과했다.
아직 **PR은 열지 않았다** — 박성우(B) 사전 공유가 남아 있다.

## 이전에 막혔던 지점 — 해소됨

이전 세션에서 실습용 키 3개가 전부 `401 invalid_api_key`로 거부됐던 문제는 **키 발급
문제였음이 확인됐다.** 새로 받은 키는 `/v1/models`·`/v1/embeddings` 둘 다 200을 받았고
(`text-embedding-3-small` 응답 차원 1536, `application.yml` 설정과 일치), 이후 인제스트·
검색·답변 전 과정이 실제로 동작했다.

## 이번 세션에서 발견한 환경 상태 — 이전 기록과 달랐던 점

이전 인수인계 문서는 "pgvector 계속 켜둔 상태", "Homebrew postgresql@17 중지해둔 상태"라고
적었지만 실제로는 **둘 다 사실과 달랐다**:

- Homebrew `postgresql@17`이 여전히 5432를 점유하며 실행 중이었다(`brew services stop`이
  이 환경의 Homebrew 버그로 먹지 않는 상태였던 것으로 보인다) → `launchctl unload -w
  /opt/homebrew/opt/postgresql@17/homebrew.mxcl.postgresql@17.plist`로 해소.
  데이터는 건드리지 않았다. 복구하려면 `launchctl load -w` 동일 경로.
- Docker 데몬이 꺼져 있었고 pgvector 컨테이너·볼륨 자체가 없었다(이전 검증 시도가 남긴
  흔적이 없다는 뜻) → `open -a Docker` 후 `docker compose up -d`로 새로 기동.

## 이번 세션에서 새로 발견하고 고친 것 — JDBC 채팅 메모리 스키마 미초기화

RAG와는 무관하지만 시나리오 ① end-to-end 검증(`POST /api/chat`)을 막고 있던 버그를
발견해 고쳤다. `JdbcChatMemoryRepository`의 스키마 자동초기화 기본값이 `embedded`라
외부 Postgres(pgvector 컨테이너)에는 `spring_ai_chat_memory` 테이블이 안 만들어져
**첫 채팅 요청부터 500**이 났다. `application.yml`에
`spring.ai.chat.memory.repository.jdbc.initialize-schema: always`를 추가해 해결.
Phase 5(JDBC 대화 메모리) 영역이라 A 소유 파일 안에서 처리했다.

## 검증 6단계 결과 (전부 통과)

1. **기동·인제스트** — `./gradlew bootRun` → `/actuator/health` UP. 6개 파일 전부 실측
   임베딩 성공: `academic-regulations.md`(2청크), `graduation-requirements.md`(2청크),
   `scholarship-policy.md`(2청크), `한국기술교육대학교_학칙.pdf`(142청크),
   `학사운영에관한규칙.pdf`(166청크), `장학금에관한규칙.pdf`(18청크).
2. **`GET /api/admin/chunks?q=졸업요건&topK=5`** — 앵커 md와 신규 PDF가 함께 잡힘, source·
   version·score 정상 반환.
3. **PII 미유입** — `q=교수 명단`은 지도교수 관련 조문만, `q=연락처`는 빈 신청서 서식만
   반환. 실명·연락처 등 개인정보 유출 없음 — 대학요람 PDF를 코퍼스에서 제외한 판단이
   실측으로도 유효했다.
4. **재색인 중복 방지** — 앱을 두 번 재시작(threshold 조정 포함 총 3회 인제스트)했고,
   매번 `docker exec ... psql`로 source별 행 수를 직접 세어 확인: 각 source당 정확히
   1세트만 존재(중복 누적 없음). `vectorStore.delete("source == '...'")` 재색인 delete가
   설계대로 동작한다.
5. **조항 구분** — `휴학`·`자퇴`·`제적` 키워드/자연어 질문 모두 서로 다른 정확한 조항으로
   구분됨(제20조 휴학신청, 제25조의2 자퇴, 제28조 제적 등).
6. **threshold 실측 조정** — 아래 별도 절 참고.

## threshold 실측 조정 — 0.5 → 0.3

`api/admin/chunks`의 단어 단위 질의와 실제 채팅 경로(자연어 전체 문장)는 유사도 스코어
스케일이 다르다는 걸 확인했다. 자연어 질문 기준 실측값:

| 질문 유형 | 최고 score |
|---|---|
| 무관 질문("오늘 학생식당 점심 메뉴가 뭐예요?") | 0.287 |
| 자퇴(정확한 조항 매치) | 0.369 |
| 제적(정확한 조항 매치) | 0.434 |
| 휴학·졸업요건(정확한 조항 매치) | 0.52~0.65 |

기존 `threshold: 0.5`는 무관 질문은 잘 걸렀지만 자퇴·제적처럼 근거가 실제로 있는 질문까지
걸러내(재현율 손실) `POST /api/chat`이 "정확한 규정을 확인할 수 없습니다"로 잘못 답했다.
`threshold: 0.3`으로 낮춘 뒤 재검증하니 무관 질문은 여전히 걸러지고(0.287 < 0.3) 자퇴·
제적도 정확한 근거로 답변했다. `chunk-size`(300)·`min-chunk-size-chars`(150)는 preview가
조항 단위를 온전히 담고 있어 조정하지 않았다.

사소한 노이즈 1건: "제적 사유가 뭐가 있나요?" 답변 출처에 `scholarship-policy.md`가
threshold 턱걸이(0.3 근처)로 낀 경우가 있었다. 답변 품질 자체는 문제없었지만, `topK`를
줄이거나 threshold를 조금 더 올리는 미세조정 여지는 남아 있다 — 다음 라운드에서
실제 사용 패턴을 보고 판단.

## `./gradlew build`

통과(`BUILD SUCCESSFUL`). `src/test`가 아직 없어(Phase 8에서 B가 작성 예정) 테스트는
`NO-SOURCE`.

## 환경 메모 (갱신)

- pgvector 컨테이너: `docker compose up -d`로 새로 켠 상태, 계속 켜둠.
- Homebrew `postgresql@17`: `launchctl unload -w`로 내려둔 상태. 이 실습과 무관하게
  쓰던 서비스라면 복구는 `launchctl load -w
  /opt/homebrew/opt/postgresql@17/homebrew.mxcl.postgresql@17.plist`로 가능.
- `.env` 파일은 없다 — API 키는 항상 `export`(또는 세션 스크래치패드에서 source)로만
  다룬다(사용자 원칙, 유지).
- **키 취급 주의** — 이번에 받은 키는 대화창에 평문으로 전달돼 대화 기록에 남았다.
  실습 종료 후 폐기(rotate)를 권고했다.

## 다음에 할 일

1. 박성우에게 아래 "전달할 내용" 공유
2. `hwangjaewon/skala-helpdesk-ai` → `main` PR 오픈(왜/무엇을/어떻게 검증했는지 3가지 기재)
3. Phase 3(`HelpDeskService#ask`의 TODO)는 이미 실동작 중이지만 Javadoc의 `TODO(A, Phase 3)`
   마커 정리는 남아 있음 — 이번 세션 범위 밖으로 남겨둠

## 박성우에게 전달할 내용 (아직 미전달 — PR 열기 전에 먼저 공유)

> 1. `HelpDeskProperties.Rag`에 `chunkSize`·`minChunkSizeChars` 필드를 추가했어(원래
>    "완성 제공, 손대지 않는다" 파일인데, "값 조정 시 application.yml과 함께 바꾼다"는
>    문서 취지 안에서 청크 파라미터를 상수로 안 남기려고 추가했어. AGENTS.md에 예외
>    사유 기록해둠).
> 2. RAG 코퍼스에 학사규정 PDF 3종(학칙·학사운영에관한규칙·장학금에관한규칙,
>    rule.koreatech.ac.kr 출처)을 추가했어. 기존 3종 md 앵커는 안 건드렸어.
> 3. `docType`이 3종에서 6종(`academic`/`학칙`/`교무행정`/`학생행정`)으로 늘었어 —
>    골든셋·검증 시나리오에서 메타데이터 필터 쓸 거면 참고해줘.
> 4. **임베딩 실측 검증 완료**. `threshold`를 0.5 → 0.3으로 조정했어(자연어 질문 기준
>    무관 질의 최고 0.287, 자퇴·제적처럼 실제 근거 있는 질문이 0.37~0.43이라 0.5는
>    재현율 손실이 있었음). `q=제적` 계열 응답에 `scholarship-policy.md`가 threshold
>    턱걸이로 낀 사례가 하나 있었는데, 심각하진 않지만 다음에 topK·threshold 미세조정
>    여지로 남겨뒀어.
> 5. **RAG와 무관한 버그 하나 발견·수정**: `JdbcChatMemoryRepository` 스키마 자동초기화
>    기본값이 `embedded`라 우리 외부 Postgres에는 `spring_ai_chat_memory` 테이블이 안
>    생겨서 첫 채팅부터 500이 났어. `application.yml`에
>    `spring.ai.chat.memory.repository.jdbc.initialize-schema: always` 추가해서 고쳤어
>    (Phase 5, A 소유 파일이라 바로 반영). 리뷰할 때 참고해줘.
> 6. `/api/admin/chunks`(B 소유, `AdminController`)가 `docType`/`dept`를 반환하지 않아
>    메타데이터 필터 확인이 어렵고, `similarityThreshold`를 안 걸어서 Advisor 경로(0.3)와
>    결과가 어긋날 수 있어 — 시간 될 때 추가해주면 좋을 것 같아. 직접 안 고쳤어(네 파일).
> 7. 대학요람 PDF는 교원 실명 명단이 있어서 레드팀 원칙과 충돌해 코퍼스에서 제외했어 —
>    이번에 `q=교수 명단`·`q=연락처`로 실측 확인했을 때도 개인정보 유출 없었어.

## PR

**아직 열지 않았다** — 박성우 사전 공유가 끝나면 연다.
